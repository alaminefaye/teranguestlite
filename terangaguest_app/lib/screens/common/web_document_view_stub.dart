import 'package:flutter/widgets.dart';

class WebDocumentView extends StatelessWidget {
  final String url;

  const WebDocumentView({super.key, required this.url});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
